列表的定义：
    python 中表示组的一种方式。

    [1, 2, 3 4, 5, 6]

    列表的操作：
        +
        *
        通过下标取值



    元组：
        定义：
            a = (1, '1', 'b', True)
            a[0]
        操作：
            +
            *
            下表访问
        和列表的区别


    序列总结:
        表示一个元素的元组: (1,)

        int , float, bool,

        str, list, tuple(这3个成为序列)
            可以通过下标访问的数据， 我们成为序列
            切片
                [:]
                [0:]
                [1:10:2], 这里之后的2为步长

